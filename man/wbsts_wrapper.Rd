% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrap-applied.R
\name{wbsts_wrapper}
\alias{wbsts_wrapper}
\title{WBS for nonstationary time series}
\usage{
wbsts_wrapper(
  x,
  n_intervals = 0,
  cstar = 0.75,
  lambda = 0.75,
  scales = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{x}{A numeric vector.}

\item{n_intervals}{Number of random intervals (\code{M}). Defaults to
\code{0}, which is the engine's "all dyadic intervals" setting.}

\item{cstar, lambda}{Post-processing constants; the engine's defaults are
\code{0.75} for both.}

\item{scales}{Wavelet scales to use. \code{NULL} lets the engine choose.}

\item{seed}{Optional seed.}

\item{...}{Additional arguments passed to \code{wbsts::wbs.lsw()}.}
}
\value{
A \code{ggcpt} object with \code{change_in = "var"}.
}
\description{
Wraps \code{wbsts::wbs.lsw()} (Korkas and Fryzlewicz): wild binary
segmentation applied to the locally stationary wavelet spectrum, so it
detects changes in the \emph{second-order} structure — variance and
autocovariance — of a nonstationary series. Where \code{wbs} looks for
jumps in the level, this looks for jumps in how the series behaves.
}
\examples{
\dontshow{if (requireNamespace("wbsts", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
\donttest{
set.seed(2026)
y <- c(as.numeric(stats::arima.sim(list(ar = 0.1), 250)),
       as.numeric(stats::arima.sim(list(ar = 0.9), 250)))
wbsts_wrapper(y)
}
\dontshow{\}) # examplesIf}
}
\references{
\insertRef{korkas2017wbsts}{ggchangepoint}
}
