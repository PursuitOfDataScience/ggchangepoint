% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrap-highdim2.R
\name{esac_wrapper}
\alias{esac_wrapper}
\title{ESAC wrapper — sparsity-adaptive high-dimensional detection}
\usage{
esac_wrapper(
  x,
  threshold_d = 1.5,
  threshold_s = 1,
  empirical = FALSE,
  N = 1000,
  seed = NULL,
  ...
)
}
\arguments{
\item{x}{A numeric matrix or data frame with rows as time points and
columns as coordinates.}

\item{threshold_d, threshold_s}{Leading constants of the dense and sparse
thresholds. Defaults follow the engine (\code{1.5} and \code{1}).}

\item{empirical}{Calibrate the thresholds by Monte Carlo rather than using
the theoretical values? Slower but sharper; defaults to \code{FALSE}.}

\item{N}{Monte Carlo samples when \code{empirical = TRUE}.}

\item{seed}{Optional seed (used by the empirical calibration).}

\item{...}{Additional arguments passed to \code{HDCD::ESAC()}.}
}
\value{
A \code{ggcpt} object. The changepoints tibble carries
  \code{cusum} (the ESAC statistic at each detected location) and
  \code{depth} (its level in the recursion).
}
\description{
Wraps \code{HDCD::ESAC()} (Moen, Glad and Tveten, 2023): Efficient
Sparsity Adaptive Changepoint estimation for a change in the mean vector
of a high-dimensional series. Where \code{inspect} projects onto a single
estimated sparse direction, ESAC adapts across the whole sparsity range at
once, which is a different regime rather than a refinement of the same one
— it is competitive both when a handful of coordinates change and when all
of them do.
}
\examples{
\dontshow{if (requireNamespace("HDCD", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
set.seed(2026)
X <- matrix(rnorm(100 * 20), nrow = 100)
X[51:100, 1:5] <- X[51:100, 1:5] + 3
esac_wrapper(X)
\dontshow{\}) # examplesIf}
}
\references{
\insertRef{moen2023esac}{ggchangepoint}
}
