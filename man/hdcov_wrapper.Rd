% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrap-highdim2.R
\name{hdcov_wrapper}
\alias{hdcov_wrapper}
\title{High-dimensional covariance changepoints}
\usage{
hdcov_wrapper(
  x,
  threshold = NULL,
  alpha = 0.05,
  n_perm = 20,
  delta = NULL,
  seed = NULL
)
}
\arguments{
\item{x}{A numeric matrix or data frame, rows as time points.}

\item{threshold}{Detection threshold on the CUSUM statistic. When
\code{NULL} (the default) it is calibrated by permutation: the time
order is shuffled \code{n_perm} times, which destroys any changepoint
while preserving the marginal distributions, and the threshold is the
\code{1 - alpha} quantile of the largest statistic seen.}

\item{alpha}{Family-wise level for the permutation threshold. Defaults to
\code{0.05}.}

\item{n_perm}{Permutations used to calibrate the threshold. Defaults to
\code{20}; raise it for a sharper threshold at proportional cost.}

\item{delta}{Minimum spacing between changepoints. Defaults to
\code{max(10, floor(n / 20))}.}

\item{seed}{Optional seed (the permutation calibration is random).}
}
\value{
A \code{ggcpt} object with \code{change_in = "covariance"}; the
  changepoints tibble carries the CUSUM statistic in \code{cusum}.
}
\description{
Wraps \code{changepoints::BS.cov()} (Wang, Yu and Rinaldo): binary
segmentation on the sample covariance operator, which detects a change in
the \emph{dependence structure} of a multivariate series even when every
marginal mean and variance is unchanged. No other engine in the package
can see that.
}
\examples{
\dontshow{if (requireNamespace("changepoints", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
set.seed(2026)
p <- 5
A <- matrix(rnorm(100 * p), ncol = p)
B <- matrix(rnorm(100 * p), ncol = p)
B[, 2] <- B[, 1] + 0.2 * B[, 2]        # correlation appears
hdcov_wrapper(rbind(A, B), n_perm = 20, alpha = 0.05, seed = 1)
\dontshow{\}) # examplesIf}
}
\references{
\insertRef{wang2021covariance}{ggchangepoint}
}
\seealso{
Other changepoint engines: 
\code{\link{bcp_wrapper}()},
\code{\link{beast_wrapper}()},
\code{\link{bfast_wrapper}()},
\code{\link{binsegrcpp_wrapper}()},
\code{\link{bocpd_wrapper}()},
\code{\link{cpm_wrapper}()},
\code{\link{cpop_wrapper}()},
\code{\link{cpt_wrapper}()},
\code{\link{decafs_wrapper}()},
\code{\link{ecp_wrapper}()},
\code{\link{envcpt_wrapper}()},
\code{\link{esac_wrapper}()},
\code{\link{fabisearch_wrapper}()},
\code{\link{fastcpd_wrapper}()},
\code{\link{fcov_wrapper}()},
\code{\link{fmean_wrapper}()},
\code{\link{fpop_wrapper}()},
\code{\link{geomcp_wrapper}()},
\code{\link{hdreg_wrapper}()},
\code{\link{idetect_wrapper}()},
\code{\link{inspect_wrapper}()},
\code{\link{kcp_wrapper}()},
\code{\link{kwc_wrapper}()},
\code{\link{mcp_wrapper}()},
\code{\link{mosum_wrapper}()},
\code{\link{network_wrapper}()},
\code{\link{not_wrapper}()},
\code{\link{npmojo_wrapper}()},
\code{\link{nsp_wrapper}()},
\code{\link{ocd_wrapper}()},
\code{\link{pilliat_wrapper}()},
\code{\link{segmented_wrapper}()},
\code{\link{smuce_wrapper}()},
\code{\link{sn_wrapper}()},
\code{\link{strucchange_wrapper}()},
\code{\link{taylor_wrapper}()},
\code{\link{tguh_wrapper}()},
\code{\link{trend_wrapper}()},
\code{\link{var_wrapper}()},
\code{\link{wbs2_wrapper}()},
\code{\link{wbs_wrapper}()},
\code{\link{wbsts_wrapper}()}
}
\concept{changepoint engines}
