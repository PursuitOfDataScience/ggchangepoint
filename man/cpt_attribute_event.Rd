% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{cpt_attribute_event}
\alias{cpt_attribute_event}
\title{Was a detected change the event you have in mind?}
\usage{
cpt_attribute_event(fit, event, level = 0.95, ...)
}
\arguments{
\item{fit}{A \code{ggcpt} object.}

\item{event}{One or more event locations: positions, values of the fit's
index, or a \code{\link{cpt_annotate_events}()} result.}

\item{level}{Confidence level of the interval. Defaults to \code{0.95}.}

\item{...}{Further arguments for \code{\link{cpt_confint}()} (for
example \code{B} or \code{seed} for a bootstrap interval).}
}
\value{
A tibble with one row per event: \code{event},
  \code{event_position}, the nearest changepoint \code{cp} (and
  \code{cp_index}), its interval \code{ci_lower}/\code{ci_upper},
  \code{distance} (positions from the changepoint to the event),
  \code{inside} and a plain-language \code{verdict}.
}
\description{
The mirror of \code{\link{cpt_test_at}()}. A detector found a change on
14 March and something is known to have happened on 1 March: is that the
same event? The detection's confidence interval answers it: an event
inside the interval is consistent with being the change, one outside is
not.
}
\examples{
\dontshow{if (requireNamespace("strucchange", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
set.seed(1)
dates <- as.Date("2026-01-01") + 0:119
fit <- cpt_detect(c(rnorm(60), rnorm(60, 3)), method = "strucchange",
                  index = dates)
cpt_attribute_event(fit, as.Date(c("2026-02-28", "2026-04-10")))
\dontshow{\}) # examplesIf}
}
\seealso{
Other inference: 
\code{\link{cpt_assumptions}()},
\code{\link{cpt_effect}()},
\code{\link{cpt_gof}()},
\code{\link{cpt_null_power}()},
\code{\link{cpt_robustness}()},
\code{\link{cpt_test_at}()},
\code{\link{cpt_test_null}()}
}
\concept{inference}
