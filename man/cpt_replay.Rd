% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitor.R
\name{cpt_replay}
\alias{cpt_replay}
\title{Replay a series through a sequential detector}
\usage{
cpt_replay(x, method = c("edetector", "cpm", "ocd"), baseline = NULL, ...)
}
\arguments{
\item{x}{A numeric vector, or a matrix with rows as time points.}

\item{method}{Sequential detector; see \code{\link{cpt_monitor}()}.}

\item{baseline}{Number of leading observations used as pre-change training
data (an integer), or an explicit baseline vector. Defaults to
\code{min(100, floor(n / 4))}.}

\item{...}{Additional arguments passed to \code{\link{cpt_monitor}()}.}
}
\value{
A \code{ggcpt_monitor} that has already consumed the series.
}
\description{
Runs a whole series through a monitor in one call, so the alarm timeline
of an online method can be studied retrospectively without hand-rolling
the update loop. This is the honest version of what 0.4.0's \code{cpm},
\code{ocd} and \code{bocpd} wrappers do implicitly: it reports
\emph{alarms}, which arrive after the change, rather than pretending they
are estimated changepoint locations.
}
\examples{
set.seed(2026)
mon <- cpt_replay(c(rnorm(200), rnorm(200, 3)), method = "edetector")
alarms(mon)
}
\seealso{
\code{\link{cpt_delay}()}, \code{\link{cpt_monitor}()}.
}
