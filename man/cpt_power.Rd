% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{cpt_power}
\alias{cpt_power}
\alias{tidy.ggcpt_power}
\alias{print.ggcpt_power}
\alias{autoplot.ggcpt_power}
\title{Detection power for a changepoint scenario}
\usage{
cpt_power(
  n,
  jump,
  sigma = 1,
  method = "pelt",
  location = 0.5,
  n_sim = 200,
  tolerance = 5,
  change_in = "mean",
  noise = "gauss",
  rho = 0,
  df = 3,
  seed = NULL,
  parallel = TRUE,
  ...
)

\method{tidy}{ggcpt_power}(x, ...)

\method{print}{ggcpt_power}(x, ...)

\method{autoplot}{ggcpt_power}(object, ...)
}
\arguments{
\item{n}{Series length.}

\item{jump}{Size of the change, in units of \code{sigma}. A vector runs
one scenario per value.}

\item{sigma}{Noise standard deviation. Defaults to \code{1}.}

\item{method}{Detection method. Defaults to \code{"pelt"}.}

\item{location}{Changepoint position, as a fraction of \code{n} in
\eqn{(0, 1)} or an integer position. Defaults to \code{0.5}. A vector
runs one scenario per value.}

\item{n_sim}{Replicates per scenario. Defaults to \code{200}.}

\item{tolerance}{A detection counts as finding the change when it falls
within this many positions of it. Defaults to \code{5}.}

\item{change_in}{What changes. Defaults to \code{"mean"}.}

\item{noise}{Noise model, passed to \code{\link{cpt_simulate}()}.}

\item{rho}{AR(1) parameter when \code{noise = "ar1"}.}

\item{df}{Degrees of freedom when \code{noise = "t"}.}

\item{seed}{Optional seed.}

\item{parallel}{Use \code{future::plan()} when \pkg{future.apply} is
available? Defaults to \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link{cpt_detect}()}.}

\item{x}{A \code{ggcpt_power} object.}

\item{object}{A \code{ggcpt_power} object (for \code{autoplot()}).}
}
\value{
A \code{ggcpt_power} object: a tibble with one row per scenario —
  \code{n}, \code{jump}, \code{sigma}, \code{location}, \code{power}
  (proportion of replicates detecting the change within
  \code{tolerance}), \code{mc_se} (the Monte Carlo standard error of that
  proportion), \code{mean_abs_error} (location error among detections),
  \code{false_positive_rate} (mean number of \emph{extra} changepoints per
  replicate) and \code{n_sim} — with \code{print()} and
  \code{autoplot()}.
}
\description{
Simulates replicate series under a scenario and reports how often the
detector finds the change, how accurately it locates it, and how often it
reports changes that are not there. This is the calculation that should
precede an analysis, and the one applied papers are increasingly asked
for.
}
\examples{
\donttest{
pw <- cpt_power(n = 200, jump = c(0.5, 1, 2), n_sim = 30, seed = 1)
pw
ggplot2::autoplot(pw)
}
}
\seealso{
\code{\link{cpt_min_detectable}()}, \code{\link{cpt_scenarios}()},
  \code{\link{cpt_simulate}()}.
}
