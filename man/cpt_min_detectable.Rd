% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{cpt_min_detectable}
\alias{cpt_min_detectable}
\alias{print.ggcpt_min_detectable}
\title{The smallest detectable change}
\usage{
cpt_min_detectable(
  n,
  sigma = 1,
  method = "pelt",
  power = 0.8,
  range = c(0.1, 5),
  n_sim = 100,
  tolerance = 5,
  change_in = "mean",
  location = 0.5,
  noise = "gauss",
  rho = 0,
  df = 3,
  tol = 0.05,
  max_iter = 12,
  seed = NULL,
  ...
)

\method{print}{ggcpt_min_detectable}(x, ...)
}
\arguments{
\item{n}{Series length.}

\item{sigma}{Noise standard deviation. Defaults to \code{1}.}

\item{method}{Detection method. Defaults to \code{"pelt"}.}

\item{power}{Target detection probability. Defaults to \code{0.8}.}

\item{range}{Search range for the change size, in standard deviations.
Defaults to \code{c(0.1, 5)}.}

\item{n_sim}{Replicates per evaluation. Defaults to \code{100}; the answer
is only as precise as this makes it, and the returned object records the
Monte Carlo interval at the solution.}

\item{tolerance}{A detection counts as finding the change when it falls
within this many positions of it. Defaults to \code{5}.}

\item{change_in}{What changes. Defaults to \code{"mean"}.}

\item{location}{Changepoint position, as a fraction of \code{n} in
\eqn{(0, 1)} or an integer position. Defaults to \code{0.5}. A vector
runs one scenario per value.}

\item{noise}{Noise model, passed to \code{\link{cpt_simulate}()}.}

\item{rho}{AR(1) parameter when \code{noise = "ar1"}.}

\item{df}{Degrees of freedom when \code{noise = "t"}.}

\item{tol}{Bisection tolerance on the change size. Defaults to
\code{0.05}.}

\item{max_iter}{Maximum bisection steps. Defaults to \code{12}.}

\item{seed}{Optional seed. The seed is scoped to this call:
\code{.Random.seed} is saved and restored, so a seeded call inside a
simulation loop does not pin the loop's own stream.}

\item{...}{Ignored.}

\item{x}{A \code{ggcpt_min_detectable} object.}
}
\value{
A list with \code{jump} (the smallest change reaching
  \code{power}), \code{achieved_power}, \code{mc_se}, and the
  \code{trace} of evaluations, with a \code{print()} method.
}
\description{
Inverts \code{\link{cpt_power}()}: searches for the change size at which
the detector reaches a target power. The study-design counterpart of a
power curve, and the number that belongs in a pre-registration.
}
\examples{
\donttest{
cpt_min_detectable(n = 200, n_sim = 30, max_iter = 4, seed = 1)
}
}
\seealso{
\code{\link{cpt_power}()}.
}
