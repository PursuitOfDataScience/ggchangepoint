% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-regions.R
\name{stat_cpt_region}
\alias{stat_cpt_region}
\title{Significance regions computed in the layer}
\usage{
stat_cpt_region(
  mapping = NULL,
  data = NULL,
  geom = GeomCptRegion,
  position = "identity",
  ...,
  method = "nsp",
  na.rm = FALSE,
  show.legend = NA
)
}
\arguments{
\item{mapping}{Aesthetic mappings.}

\item{data}{A data frame.}

\item{geom}{The geom to draw with. Defaults to
\code{\link{geom_cpt_region}()}'s.}

\item{position}{Position adjustment.}

\item{...}{Further arguments: those \code{cpt_detect()} takes (for
example \code{alpha} for NSP's level) go to the detector, the rest to
the geom.}

\item{method}{Detection method: \code{"nsp"} (the default) or any method
whose result carries regions or location intervals (see
\code{cpt_methods()$ci}).}

\item{na.rm}{If \code{FALSE}, missing values are removed.}

\item{show.legend}{Whether to show legend.}
}
\value{
A ggplot layer.
}
\description{
The region twin of \code{\link{stat_changepoint}()}: runs detection on
the layer's own \code{x}/\code{y} and draws what the method returns as
an interval, so
\code{ggplot(d, aes(t, y)) + geom_line() + stat_cpt_region()} is the
whole plot. Narrowest Significance Pursuit (\code{method = "nsp"}, the
default) returns regions, each containing a changepoint at a global
significance level; a method with location intervals (\code{"smuce"},
\code{"strucchange"}, \code{"segmented"}, ...) draws those instead.
}
\examples{
\dontshow{if (requireNamespace("nsp", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
library(ggplot2)
set.seed(2026)
d <- data.frame(t = 1:200, y = c(rnorm(100), rnorm(100, 2)))
ggplot(d, aes(t, y)) + stat_cpt_region(seed = 1) + geom_line()
\dontshow{\}) # examplesIf}
}
\seealso{
Other ggplot2 layers: 
\code{\link{geom_changepoint}()},
\code{\link{geom_cpt_ci}()},
\code{\link{geom_cpt_event}()},
\code{\link{geom_cpt_label}()},
\code{\link{geom_cpt_region}()},
\code{\link{geom_cpt_segment}()},
\code{\link{ggchangepoint-ggproto}},
\code{\link{stat_changepoint}()}
}
\concept{ggplot2 layers}
