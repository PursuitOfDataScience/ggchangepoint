% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrap-functional.R
\name{kwc_wrapper}
\alias{kwc_wrapper}
\title{Robust depth-based changepoints for functional and multivariate data}
\usage{
kwc_wrapper(
  x,
  algorithm = c("fkwc", "dwbs"),
  depth = NULL,
  change_in = c("covariance", "distribution"),
  seed = NULL,
  ...
)
}
\arguments{
\item{x}{A numeric matrix or data frame with one row per observation
(time point) and one column per grid location or coordinate.}

\item{algorithm}{\code{"fkwc"} (default; pruned exact linear time over the
depth ranks) or \code{"dwbs"} (depth-based wild binary segmentation).}

\item{depth}{Depth function. For \code{"fkwc"} one of \code{"RPD"}
(default), \code{"FM"}, \code{"LTR"}, \code{"FMd"}, \code{"RPDd"}; for
\code{"dwbs"} one of \code{"spat"}, \code{"hs"}, \code{"mahal"},
\code{"mahal75"}.}

\item{change_in}{Reported change type: \code{"covariance"} (default) or
\code{"distribution"}. The test is sensitive to both; this only labels
the result.}

\item{seed}{Optional seed — the random-projection depths and the wild
binary segmentation both randomise.}

\item{...}{Additional arguments passed to the engine.}
}
\value{
A \code{ggcpt} object.
}
\description{
Wraps \pkg{KWCChangepoint} (Ramsay and Chenouri): the functional
Kruskal–Wallis covariance test, which ranks observations by statistical
\emph{depth} and segments on the ranks. Because it never touches the
values themselves it is insensitive to heavy tails and outlying curves,
which is exactly where the moment-based functional tests degrade.
}
\examples{
\dontshow{if (requireNamespace("KWCChangepoint", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
set.seed(2026)
X <- matrix(rnorm(100 * 20), nrow = 100)
X[51:100, ] <- X[51:100, ] * 3
kwc_wrapper(X, seed = 1)
\dontshow{\}) # examplesIf}
}
\references{
\insertRef{ramsay2024kwc}{ggchangepoint}
}
