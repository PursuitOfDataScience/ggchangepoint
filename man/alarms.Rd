% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitor.R
\name{alarms}
\alias{alarms}
\alias{alarms.ggcpt_monitor}
\title{The alarm log of a monitor}
\usage{
alarms(x, ...)

\method{alarms}{ggcpt_monitor}(x, ...)
}
\arguments{
\item{x}{A \code{ggcpt_monitor} object.}

\item{...}{Ignored.}
}
\value{
A tibble with one row per alarm: \code{time} (the observation at
  which it fired, counted from the first one fed to the monitor),
  \code{statistic} and \code{threshold}.
}
\description{
Every observation at which a sequential monitor crossed its threshold, in
the order they fired. A monitor that never fired returns a zero-row tibble
rather than \code{NULL}, so the result is always safe to \code{rbind()} or
plot.
}
\examples{
set.seed(2026)
mon <- cpt_monitor("edetector", baseline = rnorm(100))
mon <- cpt_update(mon, c(rnorm(50), rnorm(50, 3)))
alarms(mon)
}
\seealso{
\code{\link{cpt_monitor}()}, \code{\link{cpt_delay}()}.
}
