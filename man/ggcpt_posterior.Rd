% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-plots.R
\name{ggcpt_posterior}
\alias{ggcpt_posterior}
\title{Posterior probability plot for Bayesian results}
\usage{
ggcpt_posterior(x, prob_threshold = NULL)
}
\arguments{
\item{x}{A \code{ggcpt} object produced by a Bayesian wrapper.}

\item{prob_threshold}{Probability cutoff drawn as a horizontal reference
line in the probability panel; defaults to the threshold recorded on
the object (or 0.5).}
}
\value{
A ggplot object (two facets sharing the x axis).
}
\description{
Draws the classic Bayesian changepoint display: the series with its
posterior mean (top panel) and the per-location posterior probability of
a changepoint (bottom panel). Works with results from
\code{\link{bcp_wrapper}()} and \code{\link{beast_wrapper}()}.
}
\examples{
\dontshow{if (requireNamespace("bcp", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
res <- bcp_wrapper(c(rnorm(60), rnorm(60, 4)), seed = 2026)
ggcpt_posterior(res)
\dontshow{\}) # examplesIf}
}
