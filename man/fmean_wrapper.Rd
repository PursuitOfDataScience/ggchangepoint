% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrap-functional.R
\name{fmean_wrapper}
\alias{fmean_wrapper}
\title{Functional mean changepoints}
\usage{
fmean_wrapper(
  x,
  statistic = c("Tn", "Mn"),
  critical = c("simulation", "resample", "welch"),
  type = c("segmentation", "single"),
  alpha = 0.05,
  robust = FALSE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix or data frame with one row per time point and
one column per grid location (the curve's resolution).}

\item{statistic}{Test statistic: \code{"Tn"} (integrated, the default) or
\code{"Mn"} (maximum).}

\item{critical}{How critical values are obtained: \code{"simulation"}
(default), \code{"resample"} or \code{"welch"}.}

\item{type}{\code{"segmentation"} (default, multiple changes) or
\code{"single"} (one change).}

\item{alpha}{Significance level. Defaults to \code{0.05}.}

\item{robust}{Use the robust (\code{"robustmean"}) statistic instead of
the classical mean one? Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{fChange::fchange()}.}
}
\value{
A \code{ggcpt} object; the changepoints tibble carries the
  engine's \code{p_value} for each location. Multivariate input is
  reduced to \strong{one series per observation by taking the
  cross-sectional mean} of the columns, and that is the series stored on
  the result: \code{autoplot()} draws it, \code{tidy()}'s
  \code{cp_value} reads it, and \code{$segments$param_estimate} and
  \code{augment()}'s \code{.fitted}/\code{.resid} are computed from it.
  It is not any one column of the input. The full input is kept in
  \code{$data_wide} for \code{autoplot(type = "coordinates")}.
}
\description{
Wraps \code{fChange::fchange()} for changes in the \emph{mean function} of
a functional time series: each observation is a curve, and the question is
when the average curve shape changes. The binary-segmentation
(\code{"segmentation"}) mode finds multiple changes; the \code{"single"}
mode runs the one-change test and reports its p-value.
}
\examples{
\dontshow{if (requireNamespace("fChange", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
\donttest{
set.seed(2026)
# 10 curves and M = 50, not 20 and 200: the example is a demonstration,
# and the wider version measured 6.7 s against CRAN's 5 s budget.
X <- matrix(rnorm(60 * 10), nrow = 60)
X[31:60, ] <- X[31:60, ] + 2
fmean_wrapper(X, M = 50)
}
\dontshow{\}) # examplesIf}
}
\references{
\insertRef{aue2018fchange}{ggchangepoint}
}
\seealso{
Other changepoint engines: 
\code{\link{bcp_wrapper}()},
\code{\link{beast_wrapper}()},
\code{\link{bfast_wrapper}()},
\code{\link{binsegrcpp_wrapper}()},
\code{\link{bocpd_wrapper}()},
\code{\link{cpm_wrapper}()},
\code{\link{cpop_wrapper}()},
\code{\link{cpt_wrapper}()},
\code{\link{decafs_wrapper}()},
\code{\link{ecp_wrapper}()},
\code{\link{envcpt_wrapper}()},
\code{\link{esac_wrapper}()},
\code{\link{fabisearch_wrapper}()},
\code{\link{fastcpd_wrapper}()},
\code{\link{fcov_wrapper}()},
\code{\link{fpop_wrapper}()},
\code{\link{geomcp_wrapper}()},
\code{\link{hdcov_wrapper}()},
\code{\link{hdreg_wrapper}()},
\code{\link{idetect_wrapper}()},
\code{\link{inspect_wrapper}()},
\code{\link{kcp_wrapper}()},
\code{\link{kwc_wrapper}()},
\code{\link{mcp_wrapper}()},
\code{\link{mosum_wrapper}()},
\code{\link{network_wrapper}()},
\code{\link{not_wrapper}()},
\code{\link{npmojo_wrapper}()},
\code{\link{nsp_wrapper}()},
\code{\link{ocd_wrapper}()},
\code{\link{pilliat_wrapper}()},
\code{\link{segmented_wrapper}()},
\code{\link{smuce_wrapper}()},
\code{\link{sn_wrapper}()},
\code{\link{strucchange_wrapper}()},
\code{\link{taylor_wrapper}()},
\code{\link{tguh_wrapper}()},
\code{\link{trend_wrapper}()},
\code{\link{var_wrapper}()},
\code{\link{wbs2_wrapper}()},
\code{\link{wbs_wrapper}()},
\code{\link{wbsts_wrapper}()}
}
\concept{changepoint engines}
