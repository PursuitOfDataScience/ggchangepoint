% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supervised.R
\name{as_cpt_labels}
\alias{as_cpt_labels}
\title{Coerce annotations to changepoint labels}
\usage{
as_cpt_labels(truth, n, margin = 5, negatives = TRUE, series = NA_character_)
}
\arguments{
\item{truth}{Integer vector of true changepoint positions.}

\item{n}{Series length.}

\item{margin}{Half-width of the positive regions. Defaults to \code{5},
matching \code{cpt_metrics()}'s default tolerance.}

\item{negatives}{Add the \code{"no_change"} regions between the positives?
Defaults to \code{TRUE}; without them a detector is never penalised for
a false positive.}

\item{series}{Optional series identifier.}
}
\value{
A \code{cpt_labels} tibble.
}
\description{
Turns a plain ground-truth changepoint set — the kind
\code{\link{cpt_metrics}()} takes — into labelled regions, so one
annotation can drive both the metric and the supervised machinery. Each
true changepoint becomes a \code{"one_change"} region of width
\code{2 * margin + 1}, and the stretches between them become
\code{"no_change"} regions.
}
\examples{
as_cpt_labels(c(50, 120), n = 200)
}
