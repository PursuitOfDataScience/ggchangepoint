% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{predict.ggcpt}
\alias{predict.ggcpt}
\title{Forecast from a segment of a segmentation}
\usage{
\method{predict}{ggcpt}(
  object,
  newdata = NULL,
  h = 1,
  segment = "last",
  models = NULL,
  level = 0.95,
  ...
)
}
\arguments{
\item{object}{A \code{ggcpt} object.}

\item{newdata}{Optional data frame of covariates (for a formula fit, or a
segment model with covariates), or of \code{time} positions.}

\item{h}{Forecast horizon, when \code{newdata} is not given: the
positions \code{n + 1} to \code{n + h}. Defaults to \code{1}.}

\item{segment}{Which segment to forecast from: \code{"last"} (the
default) or a segment number.}

\item{models}{Optional \code{\link{cpt_segment_models}()} result; built
with its defaults when omitted.}

\item{level}{Level of the prediction interval. Defaults to \code{0.95}.}

\item{...}{Further arguments for the model's \code{predict()} method.}
}
\value{
A tibble with \code{time} (when forecasting by horizon),
  \code{.pred}, and \code{.lower}/\code{.upper} when the model supplies
  a prediction interval.
}
\description{
The point of detecting a regime change is usually that the earlier
regime should no longer inform what happens next. \code{predict()} on a
\code{ggcpt} forecasts from one segment only (by default the last), with
the segment's own model.
}
\examples{
set.seed(1)
fit <- cpt_detect(c(rnorm(100), rnorm(100, 3)), method = "pelt")
predict(fit, h = 3)
}
\seealso{
Other segment models: 
\code{\link{cpt_segment_models}()}
}
\concept{segment models}
