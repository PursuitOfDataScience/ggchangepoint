% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/influence.R
\name{cpt_sensitivity}
\alias{cpt_sensitivity}
\alias{print.ggcpt_sensitivity}
\alias{tidy.ggcpt_sensitivity}
\alias{autoplot.ggcpt_sensitivity}
\title{Sensitivity of a segmentation to its tuning parameters}
\usage{
cpt_sensitivity(x, method = "pelt", over = list(), seed = NULL, ...)

\method{print}{ggcpt_sensitivity}(x, ...)

\method{tidy}{ggcpt_sensitivity}(x, ...)

\method{autoplot}{ggcpt_sensitivity}(object, ...)
}
\arguments{
\item{x}{A numeric vector (the series), or a \code{ggcpt} object, in which
case its series and method are used.}

\item{method}{Detection method. Taken from \code{x} when it is a
\code{ggcpt}.}

\item{over}{A named list of parameter vectors to sweep. Every combination
is run, so keep the grid small:
\code{list(penalty = c(5, 10, 20), minseglen = c(2, 10))}.}

\item{seed}{Optional seed.}

\item{...}{Additional arguments held fixed across the grid and passed to
\code{\link{cpt_detect}()}.}

\item{object}{A \code{ggcpt_sensitivity} object (for \code{autoplot()}).}
}
\value{
A \code{ggcpt_sensitivity} object: a list with a \code{grid}
  tibble (one row per setting: the parameter columns, \code{n_cp}, and a
  \code{cpts} list-column), the \code{data}, and the swept parameter
  names. Methods: \code{print()}, \code{tidy()} (one row per detected
  changepoint) and \code{autoplot()} (a location heatmap over the grid).
}
\description{
The parameter analogue of \code{\link{cpt_influence}()}: instead of asking
which observation drives the answer, it asks which \emph{setting} does.
Runs the detector over a grid of tuning values and reports the detected
locations for each, which is the direct answer to the commonest reviewer
question about a changepoint analysis — "is this robust to the penalty?".
}
\examples{
set.seed(2026)
x <- c(rnorm(60), rnorm(60, 3))
s <- cpt_sensitivity(x, method = "pelt",
                     over = list(penalty = c(2, 10, 40)))
s
ggplot2::autoplot(s)
}
\seealso{
\code{\link{cpt_influence}()}, \code{\link{cpt_select}()}.
}
