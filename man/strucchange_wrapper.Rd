% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrap-regression.R
\name{strucchange_wrapper}
\alias{strucchange_wrapper}
\title{Bai-Perron structural break wrapper (strucchange)}
\usage{
strucchange_wrapper(
  x,
  data = NULL,
  breaks = NULL,
  h = 0.15,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{x}{A numeric vector (mean-shift mode), or a model formula
(regression mode; supply \code{data} too).}

\item{data}{Optional data frame for formula input.}

\item{breaks}{Maximum number of breaks; when \code{NULL} the number is
chosen by BIC.}

\item{h}{Minimal segment size, as a fraction of the sample size (or an
integer count). Defaults to \code{0.15}.}

\item{conf_level}{Confidence level for the break-date intervals. Defaults
to \code{0.95}.}

\item{...}{Additional arguments passed to
\code{strucchange::breakpoints()}.}
}
\value{
A \code{ggcpt} object with \code{ci_lower}/\code{ci_upper} columns
  on the changepoints tibble.
}
\description{
Wraps \code{strucchange::breakpoints()} (Zeileis et al., 2002), the
dynamic-programming implementation of the Bai and Perron (1998, 2003)
multiple structural break estimator. Called with a bare numeric vector it
dates mean shifts (\code{y ~ 1}); called with a formula and data it dates
breaks in arbitrary regression coefficients. Break-date confidence
intervals from \code{confint()} populate \code{ci_lower}/\code{ci_upper}
and render via \code{autoplot(show_ci = TRUE)}.
}
\examples{
\dontshow{if (requireNamespace("strucchange", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
set.seed(2026)
res <- strucchange_wrapper(c(rnorm(100), rnorm(100, 3)))
res$changepoints
\dontshow{\}) # examplesIf}
}
\references{
\insertRef{bai2003computation}{ggchangepoint}

\insertRef{zeileis2002strucchange}{ggchangepoint}
}
