% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrap-applied.R
\name{bfast_wrapper}
\alias{bfast_wrapper}
\title{BFAST wrapper — breaks for additive season and trend}
\usage{
bfast_wrapper(
  x,
  frequency = 12,
  change_in = c("mean", "slope", "seasonality"),
  h = 0.15,
  season = c("harmonic", "dummy", "none"),
  max_iter = 5,
  ...
)
}
\arguments{
\item{x}{A numeric vector, or a \code{ts} — a \code{ts} is strongly
preferred, because BFAST needs the seasonal frequency and cannot guess
it. A bare vector is turned into a \code{ts} with \code{frequency}.}

\item{frequency}{Observations per season, used when \code{x} carries none.
Defaults to \code{12}.}

\item{change_in}{Which component's breaks to report: \code{"mean"} or
\code{"slope"} give the trend breaks (the usual choice),
\code{"seasonality"} gives the seasonal ones.}

\item{h}{Minimal segment size as a fraction of the series. Defaults to
\code{0.15}.}

\item{season}{Seasonal model: \code{"harmonic"} (default), \code{"dummy"}
or \code{"none"}.}

\item{max_iter}{Maximum iterations of the trend/season loop. Defaults to
\code{5}.}

\item{...}{Additional arguments passed to \code{bfast::bfast()}.}
}
\value{
A \code{ggcpt} object whose \code{fitted} column holds the
  estimated trend component (so \code{autoplot(show_fit = TRUE)} draws it)
  and whose changepoints carry \code{ci_lower}/\code{ci_upper} from
  \pkg{strucchange}'s break-date intervals when available.
}
\description{
Wraps the \pkg{bfast} family (Verbesselt et al.), the standard tool in
remote sensing and land-cover monitoring. BFAST decomposes a seasonal
series into trend and seasonal components and detects breaks in each
separately, which is the right question for satellite time series where a
shift in phenology and a shift in level mean different things.
}
\examples{
\dontshow{if (requireNamespace("bfast", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
\donttest{
set.seed(2026)
season <- rep(sin(seq(0, 2 * pi, length.out = 12)), 10)
y <- stats::ts(c(rnorm(60, 1), rnorm(60, 5)) + season,
               frequency = 12, start = c(2000, 1))
bfast_wrapper(y)
}
\dontshow{\}) # examplesIf}
}
\references{
\insertRef{verbesselt2010bfast}{ggchangepoint}
}
\seealso{
Other changepoint engines: 
\code{\link{bcp_wrapper}()},
\code{\link{beast_wrapper}()},
\code{\link{binsegrcpp_wrapper}()},
\code{\link{bocpd_wrapper}()},
\code{\link{cpm_wrapper}()},
\code{\link{cpop_wrapper}()},
\code{\link{cpt_wrapper}()},
\code{\link{decafs_wrapper}()},
\code{\link{ecp_wrapper}()},
\code{\link{envcpt_wrapper}()},
\code{\link{esac_wrapper}()},
\code{\link{fabisearch_wrapper}()},
\code{\link{fastcpd_wrapper}()},
\code{\link{fcov_wrapper}()},
\code{\link{fmean_wrapper}()},
\code{\link{fpop_wrapper}()},
\code{\link{geomcp_wrapper}()},
\code{\link{hdcov_wrapper}()},
\code{\link{hdreg_wrapper}()},
\code{\link{idetect_wrapper}()},
\code{\link{inspect_wrapper}()},
\code{\link{kcp_wrapper}()},
\code{\link{kwc_wrapper}()},
\code{\link{mcp_wrapper}()},
\code{\link{mosum_wrapper}()},
\code{\link{network_wrapper}()},
\code{\link{not_wrapper}()},
\code{\link{npmojo_wrapper}()},
\code{\link{nsp_wrapper}()},
\code{\link{ocd_wrapper}()},
\code{\link{pilliat_wrapper}()},
\code{\link{segmented_wrapper}()},
\code{\link{smuce_wrapper}()},
\code{\link{sn_wrapper}()},
\code{\link{strucchange_wrapper}()},
\code{\link{taylor_wrapper}()},
\code{\link{tguh_wrapper}()},
\code{\link{trend_wrapper}()},
\code{\link{var_wrapper}()},
\code{\link{wbs2_wrapper}()},
\code{\link{wbs_wrapper}()},
\code{\link{wbsts_wrapper}()}
}
\concept{changepoint engines}
