% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-plots.R
\name{ggcpt_runlength}
\alias{ggcpt_runlength}
\title{Run-length posterior heatmap for Bayesian online results}
\usage{
ggcpt_runlength(x, prob_floor = 0.001)
}
\arguments{
\item{x}{A \code{ggcpt} object produced by \code{bocpd_wrapper()}.}

\item{prob_floor}{Posterior probabilities below this value are not drawn
(keeps the heatmap legible). Defaults to \code{1e-3}.}
}
\value{
A ggplot object.
}
\description{
Draws the signature BOCPD graphic: the posterior distribution of the run
length (time since the last changepoint) at every observation, as a
heatmap, with the series overlaid on top. Works with results from
\code{\link{bocpd_wrapper}()}.
}
\examples{
\dontshow{if (requireNamespace("ocp", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
res <- bocpd_wrapper(c(rnorm(60), rnorm(60, 4)))
ggcpt_runlength(res)
\dontshow{\}) # examplesIf}
}
