% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrap-mcp.R
\name{mcp_wrapper}
\alias{mcp_wrapper}
\title{Bayesian formula-based changepoint regression (mcp)}
\usage{
mcp_wrapper(
  x,
  change_in = c("mean", "slope", "var"),
  n_changepoints = 1,
  model = NULL,
  prior = list(),
  iter = 3000,
  adapt = 1000,
  chains = 3,
  seed = NULL,
  ...
)
}
\arguments{
\item{x}{A numeric vector.}

\item{change_in}{Shorthand for the segment model when \code{model} is not
given: \code{"mean"} fits a change in intercept
(\code{list(y ~ 1, ~ 1)}), \code{"slope"} a change in slope
(\code{list(y ~ 1 + t, ~ 0 + t)}), \code{"var"} a change in residual
standard deviation (\code{list(y ~ 1, ~ 0 + sigma(1))}).}

\item{n_changepoints}{Number of changepoints in the shorthand model.
Defaults to \code{1}.}

\item{model}{An explicit \pkg{mcp} model: a list of formulas. Overrides
\code{change_in} and \code{n_changepoints}, and is the reason to reach
for this engine at all.}

\item{prior}{Optional named list of priors, passed to \code{mcp::mcp()}.}

\item{iter, adapt, chains}{Sampler settings, passed through.}

\item{seed}{Optional seed.}

\item{...}{Additional arguments passed to \code{mcp::mcp()}.}
}
\value{
A \code{ggcpt} object. The changepoints tibble carries the
  posterior mean location together with \code{ci_lower}/\code{ci_upper}
  from the posterior quantiles, and \code{$data$fitted} holds the
  posterior predictive mean, so \code{autoplot(show_ci = TRUE,
  show_fit = TRUE)} shows both.
}
\description{
Wraps \code{mcp::mcp()} (Lindeløv): a Bayesian multiple-changepoint
regression specified as a \emph{list of formulas}, one per segment. This
is the most expressive detector in the package — each segment can have its
own intercept, slope, variance and autocorrelation, and the changepoints
themselves get full posterior distributions rather than point estimates,
which \code{\link{ggcpt_posterior}()} already knows how to draw.
}
\section{JAGS is a system dependency}{

\pkg{mcp} samples through JAGS, a separate program installed outside R.
Having the \emph{package} is not the same as being able to \emph{run} it:
\pkg{mcp} imports \pkg{rjags}, and on some platforms \pkg{rjags}
installs happily and only fails when it looks for the JAGS library at run
time — in which case \code{mcp::mcp()} returns a fit carrying no
posterior samples, with a warning rather than an error. This wrapper
checks for that and says so plainly instead of failing several frames
later inside \code{summary()}. Everything else in the package works
without JAGS.
}

\examples{
# Not run by R CMD check: whether this works depends on a *system*
# library, and no test of installed R packages predicts that reliably --
# `rjags` can be present and still fail to find JAGS at run time.
\dontrun{
set.seed(2026)
fit <- mcp_wrapper(c(rnorm(60), rnorm(60, 4)), iter = 500, adapt = 200)
fit
}
}
\references{
\insertRef{lindelov2020mcp}{ggchangepoint}
}
\seealso{
Other changepoint engines: 
\code{\link{bcp_wrapper}()},
\code{\link{beast_wrapper}()},
\code{\link{bfast_wrapper}()},
\code{\link{binsegrcpp_wrapper}()},
\code{\link{bocpd_wrapper}()},
\code{\link{cpm_wrapper}()},
\code{\link{cpop_wrapper}()},
\code{\link{cpt_wrapper}()},
\code{\link{decafs_wrapper}()},
\code{\link{ecp_wrapper}()},
\code{\link{envcpt_wrapper}()},
\code{\link{esac_wrapper}()},
\code{\link{fabisearch_wrapper}()},
\code{\link{fastcpd_wrapper}()},
\code{\link{fcov_wrapper}()},
\code{\link{fmean_wrapper}()},
\code{\link{fpop_wrapper}()},
\code{\link{geomcp_wrapper}()},
\code{\link{hdcov_wrapper}()},
\code{\link{hdreg_wrapper}()},
\code{\link{idetect_wrapper}()},
\code{\link{inspect_wrapper}()},
\code{\link{kcp_wrapper}()},
\code{\link{kwc_wrapper}()},
\code{\link{mosum_wrapper}()},
\code{\link{network_wrapper}()},
\code{\link{not_wrapper}()},
\code{\link{npmojo_wrapper}()},
\code{\link{nsp_wrapper}()},
\code{\link{ocd_wrapper}()},
\code{\link{pilliat_wrapper}()},
\code{\link{segmented_wrapper}()},
\code{\link{smuce_wrapper}()},
\code{\link{sn_wrapper}()},
\code{\link{strucchange_wrapper}()},
\code{\link{taylor_wrapper}()},
\code{\link{tguh_wrapper}()},
\code{\link{trend_wrapper}()},
\code{\link{var_wrapper}()},
\code{\link{wbs2_wrapper}()},
\code{\link{wbs_wrapper}()},
\code{\link{wbsts_wrapper}()}
}
\concept{changepoint engines}
